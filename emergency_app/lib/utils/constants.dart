class Constants {
  static const String baseUrl =
      "http://your-backend-url.com/api"; // Update with actual backend URL

  // API endpoints
  static const String emergencyNumbersEndpoint = "/emergency-numbers";
  static const String emergencyContactsEndpoint = "/emergency-contacts";

  // Default search limit for emergency numbers
  static const int searchLimit = 20;
}
